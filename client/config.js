// Data is pre-generated into ./data/*.json by the scheduled GitHub Action,
// so the dashboard is now pure static files served from the same origin.
export const API_BASE_URL = window.location.href;

export const LATEST_ACTIVITY_COUNT = 10;
